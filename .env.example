# 复制为 .env（或 compose 环境变量）后按需修改
# API 服务端口
PORT=3000
# 数据目录（数据库 / 上传 / 备份 / 密钥）
DATA_DIR=./state
# Prisma 数据库连接（与 DATA_DIR 指向同一文件）
DATABASE_URL=file:./state/procure.db
# OCR 微服务地址与内部通信密钥
OCR_BASE_URL=http://127.0.0.1:8000
OCR_API_KEY=dev-ocr-key
# 上传大小限制（MB）
MAX_UPLOAD_MB=30
