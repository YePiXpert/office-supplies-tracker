name: CI

on:
  push:
    branches: [main]
    tags: ['v*']
  pull_request:
  workflow_dispatch:

concurrency:
  group: docker-${{ github.ref }}
  cancel-in-progress: true

jobs:
  web:
    name: 前端（构建 + 测试）
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 11
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm --filter @procure-lite/shared build
      - run: pnpm --filter @procure-lite/web typecheck
      - run: pnpm --filter @procure-lite/web test
      - run: pnpm --filter @procure-lite/web build

  server:
    name: 服务端（构建 + 测试）
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 11
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm --filter @procure-lite/shared build
      - run: pnpm --filter @procure-lite/server typecheck
      - run: pnpm --filter @procure-lite/server test
      - run: pnpm --filter @procure-lite/server build

  ocr:
    name: OCR 解析服务（测试）
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: 安装轻依赖
        working-directory: apps/ocr
        run: pip install -e ".[dev]"
      - name: 运行测试（无需 PaddleOCR）
        working-directory: apps/ocr
        run: pytest -v

  docker:
    name: 镜像（${{ matrix.name }}）构建并推送 GHCR
    runs-on: ubuntu-latest
    needs: [web, server, ocr]
    # PR 只验证构建不推送；push / 手动触发推送 ghcr.io
    permissions:
      contents: read
      packages: write
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: web
            dockerfile: deploy/Dockerfile.web
          - name: server
            dockerfile: deploy/Dockerfile.server
          - name: ocr
            dockerfile: deploy/Dockerfile.ocr
    steps:
      - uses: actions/checkout@v4
      - uses: docker/setup-buildx-action@v3
      - uses: docker/login-action@v3
        if: github.event_name != 'pull_request'
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: 计算镜像标签
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/${{ github.repository_owner }}/procure-lite-${{ matrix.name }}
          tags: |
            type=raw,value=latest,enable={{is_default_branch}}
            type=ref,event=tag
            type=sha,prefix=sha-
      - uses: docker/build-push-action@v6
        with:
          context: .
          file: ${{ matrix.dockerfile }}
          platforms: linux/amd64
          push: ${{ github.event_name != 'pull_request' }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          # GHA 构建缓存：OCR 镜像数 GB，命中缓存后增量构建只需几分钟
          cache-from: type=gha,scope=${{ matrix.name }}
          cache-to: type=gha,scope=${{ matrix.name }},mode=max
